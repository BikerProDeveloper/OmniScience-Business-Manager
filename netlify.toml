[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables for development
[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"
